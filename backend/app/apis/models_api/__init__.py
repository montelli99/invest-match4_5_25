from fastapi import APIRouter
from app.apis.models import *  # Import all models

router = APIRouter()

# Add any model-related endpoints here if needed
# For now, this file just ensures the router exists for the API
